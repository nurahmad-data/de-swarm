# de-swarm — Project Dependencies
# Install with: pip install -r requirements.txt

# LLM & Agent Orchestration
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
openai>=1.50.0

# HTTP & API
httpx>=0.27.0
requests>=2.31.0

# Config
python-dotenv>=1.0.0

# Dataset & Model Hub
datasets>=2.20.0
huggingface_hub>=0.25.0

# Evaluation
# (sqlite3 is in Python stdlib, no install needed)

# --- Training (Kaggle notebook only — install separately if needed) ---
# transformers>=4.44.0
# peft>=0.13.0
# trl>=0.12.0
# bitsandbytes>=0.44.1
# accelerate>=0.34.0
# torch>=2.4.0

# --- GGUF Conversion (local, run separately) ---
# Install llama.cpp from source: https://github.com/ggerganov/llama.cpp
